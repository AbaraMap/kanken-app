# 漢検アプリ「墨ノート」現行仕様

## 1. アプリの目的

漢検準1級の読み問題を、スマートフォンで手軽に反復学習するためのWebアプリ。紙の単語帳や学習ノートに近い落ち着いた操作感を目指す。

## 2. 対応環境

- iPhoneの縦画面を基準に設計
- PCブラウザでも利用可能
- HTML・CSS・JavaScriptで構成
- 学習データは端末のブラウザ内に保存

## 3. TOP画面

以下の機能へ移動できる。

- 読み問題
- 過去問
- 登録単語
- オプション

学習済み数、苦手数、累計正解数も表示する。中断した問題がある場合は「続きから」と「最初から」を表示する。

## 4. 出題形式

初期版は漢検準1級の「読み問題」に対応する。

- 問題数：10問・15問・20問
- 数字入力ではなくボタンで選択
- 1セット内では同じ問題を重複出題しない
- セッション開始時に全問題と出題順を決定
- 途中で不正解になっても、同じセット内では再出題しない

## 5. 出題モード

オプションから次の2種類を選択できる。

### 完全ランダム

すべての問題を同じ条件で抽選する。

### 苦手・未出題優先

問題の状態に応じて抽選確率を調整する。

- 通常：100
- 苦手：105
- 未出題：115

不正解になった問題は苦手として扱い、次回以降の出題率を上げる。正解すると通常状態へ戻る。

## 6. 回答方法

- ひらがなで入力
- カタカナ入力はひらがなとして判定
- 前後の空白は無視
- 最大2回まで回答可能
- 1回目の不正解後は再入力できる
- 2回目も不正解なら正答と解説を表示
- 空欄で確定するとスキップ扱い
- スキップは不正解として記録
- 表外読みでは、送り仮名をすべて含む回答と、表示されている送り仮名をすべて省略した回答を許容
- 送り仮名の一部だけを入力した回答は不正解
- 複数の読みが考えられる問題では、送り仮名を除いた正答文字数を、該当する漢字の上に丸印で表示

## 7. 回答後の表示

回答確定後に以下を表示する。

- 正解または不正解
- 正しい読み
- 単語の意味
- 問題分類
- 旧字体、異体字、別表記がある場合はその情報
- お気に入り登録ボタン

## 8. Result画面

すべての問題が終了したら次を表示する。

- 正答数
- 出題数
- 正答率
- 不正解だった問題
- 各問題の読みと解説

不正解問題の解説は、項目を開閉できるアコーディオン形式で表示する。

## 9. 過去問履歴

直近100問を端末内に保存する。

保存する情報：

- 単語
- 読み
- 正誤
- 解説
- 回答日時

同じ問題が複数回出題された場合も、出題1回ごとに別の履歴として保存する。漢字と読みの両方から部分一致検索できる。

## 10. 登録単語

アプリに登録されている全単語を辞書として閲覧できる。

- 漢字から検索
- 読みから検索
- 異体字・別表記から検索
- 解説をアコーディオン表示
- お気に入りのみ表示
- 苦手のみ表示
- 未出題のみ表示

## 11. お気に入り

各単語を「☆／★」でお気に入り登録できる。

お気に入り情報は出題履歴ではなく単語自体に紐付ける。同じ単語が複数回出題されても、お気に入り状態は共通となる。

## 12. ユーザーメモ

単語ごとに自分のメモを保存できる。

例：

- 自分なりの覚え方
- 間違えやすい読み
- 類義語や対義語
- 学習時の補足

メモは端末のブラウザ内に保存する。

## 13. 中断復帰

進行中のセッションを保存し、ブラウザを閉じても続きから再開できる。

保存する情報：

- 選択された問題
- 出題順
- 現在の問題位置
- 確定済みの回答
- 正誤結果
- 問題数

入力途中の文字や、回答確定前の誤入力は復元対象外とする。

## 14. 問題データ

現在は準1級向けの仮データを30問収録している。

問題データが持てる情報：

- ID
- 漢字・単語
- 読み
- 許容する別回答
- 意味
- 分類
- 旧字体・異体字・別表記
- 一字ごとの読み
- データの出典区分
- 校正済みかどうか

現在の問題データは試作用であり、本格利用前に人間による校正が必要。

## 15. データ作成方針

問題データは次の2系統を想定する。

### 学習ノート由来

手書きノートの画像から学習済みの単語を抽出し、復習用データとして登録する。

### AI生成

未知の単語に出会うため、AIで問題データの原案を作成する。

基本的な流れ：

1. AIまたはノート画像から単語候補を作る
2. 読み・意味・分類を整理する
3. 人間が辞書や教材で校正する
4. 校正済みデータとして登録する

## 16. デザイン

- 紙と墨をイメージした配色
- 問題の漢字は明朝体
- 操作部分や解説はゴシック体
- iPhone縦画面で押しやすいボタンサイズ
- 表紙から問題を開始する単語帳風の構成
- 情報カード、一覧項目、選択肢の背景色は #FFFFFF に統一
- 見出し、単語、主要操作の基準色は黒に近い #1A1A1A、補助色は #333333
- 上部ヘッダー帯の背景色は薄藤 #E9E7F5
- ヘッダーより下のアプリ背景色は #FFFFFF
- コースボタンは薄藤系（読み #E1DEF1、四字熟語 #F0EEF8）

## 17. 今後追加する機能

以下は現時点では未実装、または簡易実装となっている。

- 指の動きに追従するPage Curl
- 前の問題へページを戻す操作
- 前問の解説を次ページ下半分に残すレイアウト
- 問題データの本格的な追加と校正
- ノート画像からの単語抽出画面
- CSV・スプレッドシートからのデータ取り込み
- 四字熟語、対義語、類義語など読み以外の問題形式
- 間違えた問題だけを再出題する復習機能
- 複数端末間のデータ同期
- App Store向けアプリ化

## 18. 四字熟語問題

読み問題とは別の選択式コースとして提供する。1問につき、次の2段階で回答する。

1. 「和光（●●）」のような空欄に当てはまる二字を3つの候補から選択する
2. 完成した四字熟語に当てはまる意味を3つの候補から選択する

両方に正解した場合に、その問題を正解として記録する。回答後は完成した四字熟語、読み、意味を表示する。履歴、苦手管理、お気に入り、中断復帰の対象にも含める。初期データは10問とする。


## 追加問題データ（2026-08-29）

- 読み問題を50問追加（表外読み25問・一般読み25問）。既存30問と合わせて80問。
- 学習ノートの写真に掲載された語から四字熟語を50問追加。既存10問と合わせて60問。
- 四字熟語の意味は、写真内の手書きメモを参照せず問題用に新規作成。
- 今回の追加データは `verified: false` とし、公開前の目視確認対象として管理する。
- 四字熟語も10問・15問・20問から出題数を選択できる。


## 熟語・一字訓読みモード（2026-08-29）

- 熟語と、熟語中の一字を使った訓読みを同じ問題カードに対で表示する。
- 「① 熟語の読み」「② 一字訓読み」の2つの入力欄へ同時に回答する。
- 熟語と一字訓はそれぞれ独立して正誤判定し、履歴・苦手・お気に入りへ別項目として記録する。回答は2回まで。
- 判定後も問題画面を維持し、正解した回答欄だけを淡い緑にして、右端へ「○ 正解」を表示する。1回目の誤答欄は通常表示のまま再回答できる。2回目の回答終了時は、誤答欄を淡い赤にして右端へ「× 誤答」を表示する。
- 単語帳には「熟語の読み」「一字訓読み」を別々の項目として登録・分類する。
- 一字訓は送り仮名付きの完全な読みと、送り仮名を除いた読みの双方を正解とする。
- 初期問題は10問。追加データは `verified: false` として目視確認対象にする。


## 表外読みコース（2026-08-29）

- 読み問題のうち、カテゴリーが「表外読み」の35問だけを出題する独立コース。
- 10問・15問・20問から出題数を選べる。回答・正誤判定・履歴・苦手管理は総合読みコースと共通。


## ホーム画面のコース配置（2026-08-29）

- コースカードは1行2枚の2列で表示し、コース数に応じて段を追加する。
- カード右側の装飾用大文字は表示せず、カード内の余白を均等にする。


## 対義語・類義語コース（2026-08-29）

- 提示語に対応する対義語または類義語を3択から選ぶ。提示語の直下に「問：対義語は？」または「問：類義語は？」と明示し、対義語は赤、類義語は青で問題種別を表示する。
- 選択肢の下に「読み」の入力欄を置き、プレースホルダーで読みの対象語を毎回明記する。読みは提示語のみを問う。
- 関係語の選択と提示語の読みをまとめて確定し、回答後は両方を個別に正誤表示する。
- 読みは2回まで入力できる。1回目の確定後は関係語の選択を固定し、読みが誤答の場合のみ読みを再回答する。
- 回答後は語の組み合わせと双方の意味を表示する。
- 対義語10問・類義語10問の計20問を初期収録する。
- 履歴・苦手管理・お気に入り・単語帳に対応し、単語帳では対義語と類義語を別分類にする。
- 初期データは `verified: false` とし、内容を調整しながら拡充する。



## 表紙とスマートフォン表示（2026-08-29）

- 表紙にはホーム画面で選んだコース名を表示する。総合読みは「総合」と表記する。
- 表紙タイトルは縦書き中央配置とし、長いコース名がはみ出さないよう最大高さを制限する。
- 表紙幅は画面幅に応じて縮小し、420px以下ではコースカードの文字・余白を調整する。




## 全読み問題共通の判定ルール（2026-08-29）

- 読みの正誤判定では、入力文字列からひらがなだけを取り出して比較する。
- 全角・半角カタカナは、対応するひらがなへ変換して判定する。空白、句読点、括弧、記号、数字、漢字などは判定から除外する。
- 例：「ひらがな」」は「ひらがな」として判定する。
- この処理は読み回答だけに適用し、単語帳の検索や選択問題の語句比較には適用しない。


## 読みの入力回数表示（2026-08-29）

- すべての読み回答で、回答回数を回答前は「入力可能回数 0/2」、1回目の誤答後は「入力可能回数 1/2」の形式で表示する。対義語・類義語では読み入力欄の右上に配置する。


## 回答ボタンの文言（2026-08-29）

- 回答を確定するボタンは、問題形式を問わず「回答を送信」と表示する。

