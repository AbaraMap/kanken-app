# 漢検アプリ「墨ノート」現行仕様

## 1. アプリの目的

漢検準1級の読み問題を、スマートフォンで手軽に反復学習するためのWebアプリ。紙の単語帳や学習ノートに近い落ち着いた操作感を目指す。

## 2. 対応環境

- iPhoneの縦画面を基準に設計
- PCブラウザでも利用可能
- HTML・CSS・JavaScriptで構成
- 学習データは端末のブラウザ内に保存

## 3. TOP画面

以下の機能へ移動できる。

- 読み問題
- 過去問
- 登録単語
- オプション

学習済み数、苦手数、累計正解数も表示する。中断した問題がある場合は「続きから」と「最初から」を表示する。

## 4. 出題形式

初期版は漢検準1級の「読み問題」に対応する。

- 問題数：10問・15問・20問
- 数字入力ではなくボタンで選択
- 1セット内では同じ問題を重複出題しない
- セッション開始時に全問題と出題順を決定
- 途中で不正解になっても、同じセット内では再出題しない

## 5. 出題モード

オプションから次の2種類を選択できる。

### 完全ランダム

すべての問題を同じ条件で抽選する。

### 苦手・未出題優先

問題の状態に応じて抽選確率を調整する。

- 通常：100
- 苦手：105
- 未出題：115

不正解になった問題は苦手として扱い、次回以降の出題率を上げる。正解すると通常状態へ戻る。

## 6. 回答方法

- ひらがなで入力
- カタカナ入力はひらがなとして判定
- 前後の空白は無視
- 最大2回まで回答可能
- 1回目の不正解後は再入力できる
- 2回目も不正解なら正答と解説を表示
- 空欄で確定するとスキップ扱い
- スキップは不正解として記録
- 一部の表外読みでは送り仮名の有無を許容

## 7. 回答後の表示

回答確定後に以下を表示する。

- 正解または不正解
- 正しい読み
- 単語の意味
- 問題分類
- 旧字体、異体字、別表記がある場合はその情報
- お気に入り登録ボタン

## 8. Result画面

すべての問題が終了したら次を表示する。

- 正答数
- 出題数
- 正答率
- 不正解だった問題
- 各問題の読みと解説

不正解問題の解説は、項目を開閉できるアコーディオン形式で表示する。

## 9. 過去問履歴

直近100問を端末内に保存する。

保存する情報：

- 単語
- 読み
- 正誤
- 解説
- 回答日時

同じ問題が複数回出題された場合も、出題1回ごとに別の履歴として保存する。漢字と読みの両方から部分一致検索できる。

## 10. 登録単語

アプリに登録されている全単語を辞書として閲覧できる。

- 漢字から検索
- 読みから検索
- 異体字・別表記から検索
- 解説をアコーディオン表示
- お気に入りのみ表示
- 苦手のみ表示
- 未出題のみ表示

## 11. お気に入り

各単語を「☆／★」でお気に入り登録できる。

お気に入り情報は出題履歴ではなく単語自体に紐付ける。同じ単語が複数回出題されても、お気に入り状態は共通となる。

## 12. ユーザーメモ

単語ごとに自分のメモを保存できる。

例：

- 自分なりの覚え方
- 間違えやすい読み
- 類義語や対義語
- 学習時の補足

メモは端末のブラウザ内に保存する。

## 13. 中断復帰

進行中のセッションを保存し、ブラウザを閉じても続きから再開できる。

保存する情報：

- 選択された問題
- 出題順
- 現在の問題位置
- 確定済みの回答
- 正誤結果
- 問題数

入力途中の文字や、回答確定前の誤入力は復元対象外とする。

## 14. 問題データ

現在は準1級向けの仮データを30問収録している。

問題データが持てる情報：

- ID
- 漢字・単語
- 読み
- 許容する別回答
- 意味
- 分類
- 旧字体・異体字・別表記
- 一字ごとの読み
- データの出典区分
- 校正済みかどうか

現在の問題データは試作用であり、本格利用前に人間による校正が必要。

## 15. データ作成方針

問題データは次の2系統を想定する。

### 学習ノート由来

手書きノートの画像から学習済みの単語を抽出し、復習用データとして登録する。

### AI生成

未知の単語に出会うため、AIで問題データの原案を作成する。

基本的な流れ：

1. AIまたはノート画像から単語候補を作る
2. 読み・意味・分類を整理する
3. 人間が辞書や教材で校正する
4. 校正済みデータとして登録する

## 16. デザイン

- 紙と墨をイメージした配色
- 問題の漢字は明朝体
- 操作部分や解説はゴシック体
- iPhone縦画面で押しやすいボタンサイズ
- 表紙から問題を開始する単語帳風の構成

## 17. 今後追加する機能

以下は現時点では未実装、または簡易実装となっている。

- 指の動きに追従するPage Curl
- 前の問題へページを戻す操作
- 前問の解説を次ページ下半分に残すレイアウト
- 問題データの本格的な追加と校正
- ノート画像からの単語抽出画面
- CSV・スプレッドシートからのデータ取り込み
- 四字熟語、対義語、類義語など読み以外の問題形式
- 間違えた問題だけを再出題する復習機能
- 複数端末間のデータ同期
- App Store向けアプリ化
